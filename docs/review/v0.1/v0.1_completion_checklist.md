# DogFootRuler v0.1 Completion Checklist
작성일: 2026-03-02

## 구현 완료 항목
- `Project`, `ProjectManager`, `PathPolicy` 도입
- `system.yaml` / `project.yaml` 기반 다중 프로젝트 관리
- Telegram 인터페이스에서 `/project_create`, `/project_use`, `/project_list` 지원
- CLI 인터페이스에서 `dogfoot project create/use/list` 지원
- task meta에 `project_name`, `project_root`, `task_dir` 저장
- task 실행/적용/커밋/머지가 task의 project context 기준으로 동작
- hard deny / root escape / allow lock mode 정책 검증 적용
- startup validation 도입
- `src/` 기반 계층 구조 리팩터링

## 테스트 완료 항목
- Unit test
  - policy allow/deny/root escape
  - hard deny / symlink escape / path normalize
  - project manager create/use/list
- Integration test
  - 임시 git repo 기반 diff / changed_files / policy 검증
  - TaskRunner 성공 / 정책 위반 실패 / 취소 / no-op 실패
- Interface test
  - Telegram project/task handler 일부 mock 기반 검증
  - CLI create/use/list 검증

## 검증 결과
- 전체 테스트: `31 passed`
- 샘플 CLI 검증:
  - `project create demo --template python`
  - `project use demo`
  - `project list`
  - 결과: sample project 생성 및 active project 전환 확인

## 남은 항목
- 실제 Telegram polling 기반 end-to-end 수동 검증
- Telegram handler 세부 단위 테스트 확대
- 운영 문서에 실제 배포/실행 절차 구체화

## 판정
- v0.1 핵심 구조와 검증 기반은 구현 완료
- 운영 완료 판정은 실제 Telegram 수동 검증까지 포함하면 더 명확해짐
