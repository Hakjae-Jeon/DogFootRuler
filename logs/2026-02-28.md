# 2026-02-28
- README.md와 문서 묶음을 확인해 프로젝트 개요와 단계별 목적을 파악함
- config/telegram.yaml에서 봇 토큰을 확인하고 `.gitignore`에 해당 경로를 등록해 커밋되지 않도록 처리함
- log 기록 방식 지시를 반영하여 최초 로그를 이 파일에 작성함
- PR1용 Python bot 구조(`bot/main.py`, `requirements.txt`)와 Task 기록/상태 로직을 구현 중. 자연어 요청은 Task ID를 생성하고 `runs/`에 아카이브함.
- allowed_users.yaml 샘플을 `allowed_users.sample.yaml`로 추가하고, 설치 시 복사해서 사용하도록 안내합니다.
- `.venv`을 --without-pip 옵션으로 생성하고 `get-pip.py`로 pip를 설치한 뒤, `.venv/bin/pip install -r requirements.txt`을 통해 `python-telegram-bot` 의존성만 설치했습니다.
- `timeout 5 .venv/bin/python bot/main.py`로 봇을 실행해 성공적으로 `/getMe`, `/deleteWebhook` 요청까지 처리되었고, 명령 제한으로 인해 종료(타임아웃)되었지만 봇이 정상적으로 시작됨이 확인되었습니다.
- `.venv/bin/python bot/main.py`를 timeout 5초로 실행해 시작 로그(`Allowlist loaded`, `/getMe`, `/deleteWebhook`, `/getUpdates`)가 찍히는 것을 확인했고, 타임아웃 조건(수동 종료)이 발생해 애플리케이션이 종료되었습니다.
- 이후 실제 운영에서는 명령 취급이 필요하다면 timeout 없이 계속 실행하면 됩니다.
- PR2 작업으로 순차 큐 워커와 status/metadata 갱신 기능을 구현했습니다. task 생성 시 메타를 `runs/<task_id>/meta.json`에 JSON 포맷으로 쓰고 큐에 넣어 queue_worker가 순차적으로 `stdout.log`, `summary.md`를 만들어 DONE 상태로 돌려놓습니다.
- 작업 로그에 전략을 남기고 `.gitignore`에 `config/allowed_users.yaml`을 추가해 설정 파일들이 버전 관리에 올라가지 않도록 했습니다.
- `runs/`를 `.gitignore`에 추가해 Task 아카이브가 버전관리되지 않도록 했습니다. PR2 작업 중에 생성된 임시 아카이브는 로컬에만 남습니다.
- queue_worker가 `post_init`에서 시작되고 `stop_queue_worker`에서 깔끔하게 cancel되도록 강화했으며, `.venv/bin/python bot/main.py` 실행 시 `PTBUserWarning`이 뜨는데 이는 봇 실행 직전에 `Application.create_task`로 백그라운드를 등록하기 때문에 나오는 경고이며, 실제로는 graceful shutdown 과정에서 취소/완료됩니다.
- PR3 구현으로 `codex exec`를 subprocess로 호출하고 stdout/stderr를 `runs/<task_id>`에 저장하며 summary.md에는 return code와 stderr 요약을 적도록 했습니다. 실패/예외 시 meta.json에 상태/return_code/notes를 기록하고 queue_worker에서 graceful cancel을 유지합니다.
- PR4에서는 task마다 `dfr/task/<task_id>` 브랜치를 생성(기존 브랜치 없으면 `git branch`)하고 `diff.patch`를 `runs`에 남기며 `/diff` 명령 처리도 추가했습니다. diff 명령은 생성된 patch를 문서로 전송하며, 없으면 “변경 없음” 메시지를 보냅니다.
- PR5 승인 흐름 구현: /apply (diff 적용), /commit (branch commit), /merge (main merge) 명령을 추가하고 task meta 상태를 READY_TO_APPLY → APPLIED → COMMITTED → MERGED로 업데이트하게 했으며, `TaskStore.status_summary`에서도 이 상태를 보여줍니다.
- PR6 안정화: /cancel 명령으로 QUEUED/RUNNING 작업을 취소하고, cancel 시 subprocess를 종료해 상태를 CANCELED로 표시하도록 했습니다. /logs 명령은 `artifacts.zip`(요약/stdout/stderr/diff 포함)을 만들어 전송하고, 실행 로그/summary에는 `mask_sensitive`로 token/key/secret 패턴을 `***`로 마스킹합니다. codex 실행에는 180초 timeout을 두고 reason을 기록하며, cancel/timeout 동안 작업 요약에 “실행 노트”를 남깁니다.
