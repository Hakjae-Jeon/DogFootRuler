# 2026-03-01
- README.md에 `policy/` 하위 문서를 항상 우선 확인하고 준수해야 한다는 규칙을 추가하고 원격 저장소에 반영함.
- `docs/plan/v0.1/` 문서 묶음을 추가하고, task-project 바인딩, active_project 전환 규칙, hard deny 강제, 경로 탈출 테스트 요구를 보강해 원격 저장소에 반영함.
- `docs/requirements/DogFootRuler_Requirements_260301-01.md`를 확인해 v0 이후 기준이 다중 프로젝트 관리, 정책 엔진, TDD 기반 리팩터링으로 전환되었음을 반영함.
- `dogfoot/` 패키지를 추가해 `PathPolicy`, `Project`, `ProjectManager`, `SystemConfig`, 간단한 YAML 로더/덤퍼를 구현함.
- `tests/test_policy.py`, `tests/test_manager.py`를 추가해 root escape, hard deny, allow 잠금 모드, symlink 우회 차단, 프로젝트 생성/선택 동작을 검증함.
- `.venv/bin/python -m pip install -r requirements.txt`로 `pytest`를 설치하고 `.venv/bin/python -m pytest tests` 실행 결과 11개 테스트가 모두 통과함.
- `bot/main.py`를 v0.1 도메인 계층과 연결해 `system.yaml` 기반 `ProjectManager` 로드, `/project_list` `/project_use` `/project_create` 명령 추가, task 생성 시 `project_name`/`project_root`를 meta에 고정 저장하도록 수정함.
- task 실행, diff/apply/commit/merge가 task meta의 `project_root` 기준으로 동작하도록 바꾸고, 정책 위반 시 `Project.assert_changes_allowed()`로 차단 후 FAILED 처리하도록 연결함.
- `.venv/bin/python -m py_compile bot/main.py $(find dogfoot -type f -name '*.py' | sort)`와 `.venv/bin/python -m pytest tests`를 다시 실행해 문법 오류 없이 기존 11개 테스트가 통과함.
- 소스 레이아웃을 `src/` 중심으로 재정리해 코어 로직은 `src/dogfoot/`, 인터페이스 어댑터는 `src/dogfoot/interfaces/`, Telegram 실행 진입점은 `bot/main.py`로 분리함.
- `bot/main.py`는 `src/dogfoot/interfaces/telegram_bot.py`를 호출하는 얇은 엔트리포인트만 남기고, 테스트는 `tests/conftest.py`에서 `src/`를 import path에 추가하도록 정리함.
- 구조 변경 후 `.venv/bin/python -m py_compile bot/main.py $(find src -type f -name '*.py' | sort)`와 `.venv/bin/python -m pytest tests`를 실행해 새 레이아웃에서도 11개 테스트가 통과함.
- `src/dogfoot/tasks/models.py`, `src/dogfoot/tasks/store.py`로 task 상태/저장소를 분리하고, `src/dogfoot/integrations/git_client.py`, `src/dogfoot/integrations/codex_runner.py`로 외부 프로세스 실행을 분리함.
- `src/dogfoot/application/artifacts.py`, `src/dogfoot/application/task_runner.py`를 추가해 마스킹/아카이브/작업 실행 워커를 인터페이스 레이어 밖으로 이동시킴.
- `src/dogfoot/interfaces/telegram_bot.py`는 Telegram 명령 핸들러와 서비스 조립 역할만 남기도록 재구성했고, 모듈 import 확인(`import dogfoot.interfaces.telegram_bot`)과 테스트 실행 결과가 정상임을 확인함.
- PR1 범위로 `tests/test_integration.py`와 `pytest.ini`를 추가해 임시 git repo 기반 integration test를 도입함. 브랜치 diff/changed_files 추출, 정책 위반 감지, task archive의 프로젝트별 runs 저장을 검증하도록 구성함.
- PR2 범위로 `tests/test_task_runner_integration.py`를 추가해 fake CodexRunner를 사용한 TaskRunner 성공/정책 위반 실패/사용자 취소 흐름을 통합 테스트로 검증함.
- PR2 테스트 과정에서 실제 버그 2개를 수정함: `git clean -fd`가 project `runs/` 아카이브를 삭제하던 문제를 `-e runs`로 보존하도록 변경했고, diff/changed_files 계산이 커밋 차이만 보던 문제를 `main` 대비 작업 트리 기준으로 수정함.
- 수정 후 `.venv/bin/python -m pytest tests` 결과 17개 테스트가 모두 통과함.
